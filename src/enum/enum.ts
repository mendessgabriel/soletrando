export enum keyboardLetterCollor {
    red = 'red',
    green = 'green',
    yellow = 'yellow'
}

export enum ModalContent {
    howToPlay = 0,
    endGameScreen = 1
}

export enum Theme {
    default = 'Padrão',
    sports = 'Esportes',
    objects = 'Objetos',
    names = 'Nomes',
}